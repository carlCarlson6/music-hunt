import styled from "@emotion/styled";

export const ContainerAboutUs = styled.div`
    text-align: left;
    max-width: 93rem;
    margin: 3rem auto 0 auto;

    a {
        color: var(--mainColor);
    }
`;